syntax = "proto3";

package example;

message Request {
  string message = 1;
}

message Response {
  string message = 1;
}

service Greeter {
  rpc SayHello (Request) returns (Response);
}
